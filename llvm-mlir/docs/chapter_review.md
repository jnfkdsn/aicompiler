# MLIR 旧章节阅读组织审查

日期：2026-09-13。依据：[工作区写作方法](../../../../writing_method.md)。本次通查目录与章节结构，重点阅读各章的开头、主例、关键机制段落；对象模型、类型/属性、效果模型额外检查了完整正文。这是讲解组织审查，不声称完成全部源码的正确性审计。

## 结论与处理范围

旧章仍有值得修改的地方，但不需要全部推倒重写。优先改善最早建立心智模型的章节，以及从事实直接跳到接口名称的段落。已经有连续执行推演的章节保留主结构。

本次完成两处局部补写：类型/属性第 1 节补上从“7 如何进入加法”到静态内容与 SSA 结果的推导；效果模型第 4 节补上“中间 store 为什么阻止 load 复用”到接口查询的判断过程。其余结构调整列为后续编辑项，不将建议表误记为改写已完成。

用户已完成的阶段 A 维持完成状态。旧章改进服务于后续查阅和课程质量，不要求重新阅读全部基础文档。PatternRewriter 新写法已获用户肯定；不能由此推断深入驱动篇或独立 Pass 实验已经完成。

## 按优先级修改

| 优先级 | 章节与具体位置 | 阅读障碍 | 建议的下一次编辑 | 本次状态 |
|---|---|---|---|---|
| 高 | `core/ir_model.md` 第 1—4 节 | 容器例子出现前先接触 bytecode、Properties、Context、完整对象定义 | 用现有 `add_one` 与容器树开篇；由隐藏入口 Block 引出三层结构；再增加循环；序列化/Context 与 Graph 细节后置 | 已审查，整章重排待做 |
| 高 | `core/types_attributes.md` 第 1、2、5、6 节 | 表格能帮助分类，但 Attribute 与 Value 的关系尚缺推导；类型家族在用途建立前集中出现 | 从 constant 的数据传递出发，再改变位宽、形状；Properties 紧接同一 constant 的 generic 输出；完整类型表作参考 | 第 1 节已补推导；后续重排待做 |
| 高 | `core/effects.md` 第 3—4 节 | SSA、tensor、描述符/生命周期之间切换较快；效果表之后直接报出接口名 | 以两次 load 能否合并贯穿前半章；先给决定过程，再说明接口提供哪项事实；descriptor 留给内存章节 | 第 4 节已补决策过程；整章串联待做 |
| 中 | `compiler/transforms/ir_api.md` 第 2—3、5—8 节 | Registry、Context、RAII、cast 与完整观察代码延迟了第一次修改结果 | 从已取得的 `fn` 开始，先走通一个 use 的 RAUW/erase；对象拥有关系在旧句柄失效处解释；独立程序初始化后置 | 已审查，待重排；保留现有观察工具 |
| 中 | `core/values_ssa.md` 第 1—4 节 | 定义与表格在前，但支配反例→合流修复已经有连续性 | 把 `double` 例子提前；保留“走右分支缺定义→沿边传参数”的核心推演；循环与层次支配后续扩展 | 已审查，局部重排即可 |
| 中 | `dialects/scf/if.md` 第 1—2 节 | 先给零/多结果、空 Region、捕获等完整契约，再看一次执行 | 用 `adjust(x=10)` 两条路径开篇，由分支局部值引出父操作结果，再归纳契约 | 已审查，待局部重排 |
| 中 | `dialects/scf/while.md` 第 1—3 节 | 两区域协议和术语较早，但现有执行表有效 | 先追计数到 2，再给 before/after 的通用状态图；A/B 两套类型作为第二步扩展 | 已审查，待局部重排 |
| 中 | `dialects/arith.md` 第 4—5 节 | poison/UB/浮点重排主要是结论，缺少位模式或舍入的具体推演 | 补一个小位宽回绕/溢出标志对照，以及一个浮点结合次序反例；将操作表保留为参考 | 已审查，专题补写待做 |
| 低 | `core/symbols_scopes.md` | forward call 与嵌套查找失败已经围绕同一问题；查表过程还能更直观 | 为嵌套 module 画“引用点→最近表→查找结果”的路径；精简重复边界提醒 | 已审查，保留主结构 |
| 低 | `dialects/cf.md` | 首节契约表略早，后面的回边逐轮表已有推演 | 合流实例前置；将分支参数与目标参数逐项连线；不重复 SSA 章定义 | 已审查，保留主结构 |
| 低 | `dialects/func.md` | 首节先给分类表，但调用数据传递已经具体 | 给 `use_pair` 一组实参，追到 callee 再回 call result；签名表随后总结 | 已审查，保留主结构 |
| 低 | `dialects/builtin.md` | module 入门与未兑现 cast 的转换专题跨度较大 | 保留作参考；开始阶段只要求读 module，cast 在 conversion 时回访 | 已审查，无需扩写成教程 |
| 保留 | `dialects/scf/for.md` | 无结果循环→累加状态→执行表→CF 已有主线 | 精简重复提醒即可；不要因为标题像问题就判定不系统 | 结构保留 |
| 保留 | `compiler/transforms/passes.md` | CSE→canonicalize→嵌套调度有输入输出与因果过程 | 更新后续链接；避免再加入大量配置打断主线 | 结构保留 |
| 保留 | `tutorials/reading_ir.md` | 一个正数求和算法连接结构、状态、执行、不变量与 lowering | 作为贯通教程样例保留；环境/验证元信息可集中到末尾 | 结构保留 |
| 保留 | `guides/inspecting_ir.md` | 文档职责是工具指南，分类与命令组织适合查用 | 不强制套用原理章叙事；保持命令与结果对应 | 结构保留 |
| 保留 | `compiler/transforms/rewriting.md` | 用户确认重写后主题和主线清楚 | 作为本次方法总结的已接受实例 | 已获反馈 |
| 深入参考 | `compiler/transforms/rewrite_drivers.md` | 多种机制仍较密集，但已从首次概念教学中分离 | 首次先读规则协作与实际轨迹，benefit/fold/预算按需查；后续可再改进节间连接 | 保留，未标记为学习完成 |

## 后续编辑顺序

需要回访基础文档时，先重排对象模型，再整理类型/属性与效果主线，然后处理 IR API 的初始化和第一次修改之间的距离。每次改写保持例子标记稳定，运行适当文档验证，避免连带破坏已验证的输入输出。

下一篇“实现并测试一个小 Pass”直接应用新方法。它属于多个机制的贯通，正文放 `tutorials/first_pass.md`；代码与测试在 `04-small-pass/`。先提供完整可读的过程与作者验证，再由读者完成有限修改任务，不把作者已实现的工程记作读者的独立成果。

## 2026-09-14：IR 定义章节修订复核

依据 `writing_method.md` 第 3—7、10—11 节，对已生成章节逐篇调整。技术验证由 `validate_ir_definition.py` 与博客构建单独提供，本记录只说明阅读组织判断。

| 章节 | 核心过程与首次结果 | 本次调整 | 保留边界 |
|---|---|---|---|
| Trait / Interface | 从未使用操作的删除判断，到操作提供范围、消费者取得 [-4,7] | 首份输出只摘录 dead；新增仅使用 i32/整数属性的直接实现，完整消费后再讲外部模型；接口目录与注册扩展细节后置 | 范围报告不实现完整范围分析 |
| Type / Attribute | 静态计算要求与结果保证 → 参数及字段验证 → 类型身份与构造失败 | uniquing 与 checked 代码分开出现；第 7 节只沿重复区间推演类型推导；Properties/复杂参数所有权移入折叠附录 | InferType 与高级存储仍为后续实现 |
| Region | 输入 → Block 参数 → yield → 父结果，先推演输入 12 得到 7，再定义约束 | 正文收束于区域传值、隔离和验证；移出 identity parser/printer；保留必要区域接口边界 | 输入数值表为语义推演，尚无 scope lowering |
| 解析与打印 | identity 文本 → OperationState 字段 → verifier → printer → 往返 | 独立成章；增加 parser 每一步得到的信息及状态变化表；以属性丢失说明为何仅解析成功不够 | 简单格式用于理解手写入口，真实同类语法优先声明式格式 |

上述修订已落实；是否进一步改善实际阅读体验仍由后续阅读反馈确认，不将作者审查结论记作用户已掌握。
